<?xml version="1.0" encoding="utf-8" ?>
<pages:BaseContentPage  xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:netxapp.Pages"
             x:Class="netxapp.Pages.LoginPage"
             Title="{Binding Title}">
    <VerticalStackLayout WidthRequest="500" Margin="100" Spacing="16">
        <Label Text="Welcome!" HorizontalTextAlignment="Center" FontSize="25"/>
        <Entry Text="{Binding Username}" Placeholder="User Name"/>
        <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Password"/>
        <Button Text="Login" HorizontalOptions="Center" Command="{Binding ExecuteLogin}" />
    </VerticalStackLayout>
</pages:BaseContentPage>